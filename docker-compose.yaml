services:
  energy-dashboard:
    build: .
    container_name: epilykos
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - SETTINGS_PASSWORD=${SETTINGS_PASSWORD}
      - TZ=${TZ:-Africa/Lagos}
      - LOG_LEVEL=${LOG_LEVEL}
