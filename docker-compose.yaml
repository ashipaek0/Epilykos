services:
  epilykos:
    image: irunmole/epilykos:latest
    container_name: epilykos
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      # Host time zone — used for "today", daily totals and savings when TZ
      # below is not set.
      - /etc/localtime:/etc/localtime:ro
    environment:
      - SETTINGS_PASSWORD=
      # Time zone for day boundaries. Remove this line to follow the host clock.
      - TZ=Africa/Lagos
      - BMS_BRIDGE_URL=http://172.17.0.1:8020
    depends_on:
      - bms-bridge
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"     # RS232 serial passthrough
    group_add:
      - "dialout"                        # Serial port permissions
    networks:
      - epilykos-net

  bms-bridge:
    image: irunmole/epilykos-bms:latest
    container_name: bms-bridge
    restart: unless-stopped
    network_mode: "host"
    privileged: true
    volumes:
      - /var/run/dbus:/var/run/dbus
    environment:
      - LOG_LEVEL=debug

networks:
  epilykos-net:
    driver: bridge
